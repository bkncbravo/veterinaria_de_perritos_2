<%= form_for(@servicio) do |f| %>
  <% if @servicio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@servicio.errors.count, "error") %> prohibited this servicio from being saved:</h2>

      <ul>
      <% @servicio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tiposervisio %><br>
    <%= f.text_field :tiposervisio %>
  </div>
  <div class="field">
    <%= f.label :tarifa %><br>
    <%= f.number_field :tarifa %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
